apiVersion: v1
kind: Service
metadata:
  name: tasker-frontend
  labels:
    app: tasker-frontend
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: tasker-frontend
