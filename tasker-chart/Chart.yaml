apiVersion: v2
name: tasker
description: Helm chart da aplicação Tasker (frontend Next.js + backend Fastify + PostgreSQL)
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: db
    version: "0.1.0"
  - name: backend
    version: "0.1.0"
  - name: frontend
    version: "0.1.0"
